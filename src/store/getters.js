export default {

    users(state) {
        return state.users;

    },
    currentUser(state) {
        return state.currentUser;

    },
    multipleSelection (state) {
        return state.multipleSelection;

    }

}
